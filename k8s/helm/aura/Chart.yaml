apiVersion: v2
name: aura
description: >
  Aura Financial Tracker — Helm chart deploying both the secure and
  vulnerable versions (each in its own namespace) plus an optional
  Prometheus/Grafana monitoring stack.
type: application
version: 1.0.0
appVersion: "1.0.0"
keywords:
  - aura
  - flask
  - security-training
