{{- if .Values.secure.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: mysql-secure-credentials
  namespace: {{ .Values.secure.namespace }}
type: Opaque
stringData:
  MYSQL_ROOT_PASSWORD: {{ .Values.secure.mysql.rootPassword | quote }}
  MYSQL_USER: {{ .Values.secure.mysql.user | quote }}
  MYSQL_PASSWORD: {{ .Values.secure.mysql.password | quote }}
  MYSQL_DATABASE: {{ .Values.secure.mysql.database | quote }}
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-secure-data
  namespace: {{ .Values.secure.namespace }}
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: {{ .Values.secure.mysql.storage }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql-secure
  namespace: {{ .Values.secure.namespace }}
  labels: { app: mysql-secure }
spec:
  replicas: 1
  strategy: { type: Recreate }
  selector:
    matchLabels: { app: mysql-secure }
  template:
    metadata:
      labels: { app: mysql-secure }
    spec:
      containers:
        - name: mysql
          image: mysql:8.0
          ports: [{ containerPort: 3306 }]
          envFrom:
            - secretRef: { name: mysql-secure-credentials }
          volumeMounts:
            - { name: data, mountPath: /var/lib/mysql }
            - { name: init-scripts, mountPath: /docker-entrypoint-initdb.d, readOnly: true }
          resources:
            {{- toYaml .Values.secure.mysql.resources | nindent 12 }}
          startupProbe:
            exec:
              command: ["mysqladmin", "ping", "-h", "127.0.0.1", "-u", "root", "-p$(MYSQL_ROOT_PASSWORD)"]
            periodSeconds: 5
            failureThreshold: 60 # up to 5 minutes for first-time init before liveness/readiness kick in
          livenessProbe:
            exec:
              command: ["mysqladmin", "ping", "-h", "127.0.0.1", "-u", "root", "-p$(MYSQL_ROOT_PASSWORD)"]
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            exec:
              command: ["mysqladmin", "ping", "-h", "127.0.0.1", "-u", "root", "-p$(MYSQL_ROOT_PASSWORD)"]
            initialDelaySeconds: 10
            periodSeconds: 5
      volumes:
        - name: data
          persistentVolumeClaim: { claimName: mysql-secure-data }
        - name: init-scripts
          configMap: { name: mysql-secure-init } # generated out-of-band, see k8s/README.md
---
apiVersion: v1
kind: Service
metadata:
  name: mysql-secure
  namespace: {{ .Values.secure.namespace }}
spec:
  selector: { app: mysql-secure }
  ports: [{ port: 3306, targetPort: 3306 }]
{{- end }}
